# Beispiel: meta
class: meta
tasks:
  smoke: "echo 'Meta repository smoke test'"
  guard: "echo 'Meta repository guard check'"
  metrics: "# TODO: repo-spezifische Metriken hier ergänzen"
  snapshot: "# TODO: tiefergehender Test-/Build-Run"

# Alternative (optional, falls just + gh CLI vorhanden sind):
# tasks:
#   smoke: "just rec-smoke"
#   guard: "just rec-guard"
#   metrics: "gh workflow run metrics.yml"
#   snapshot: "./scripts/wgx-metrics-snapshot.sh --json --output metrics.json"
