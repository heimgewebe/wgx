# Beispiel: python-service
class: python-service
tasks:
  smoke: "just rec-smoke"
  guard: "gh workflow run wgx-guard.yml"
  metrics: "gh workflow run metrics.yml"
  snapshot: "./scripts/wgx-metrics-snapshot.sh --json --output metrics.json"
