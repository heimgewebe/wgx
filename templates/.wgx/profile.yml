wgx:
  apiVersion: v1
  requiredWgx: "^2.0"
  repoKind: "generic"
  tasks:
    test: "bats -r tests"
    lint: "shfmt -d $(git ls-files '*.sh' '*.bash') && shellcheck -S style $(git ls-files '*.sh' '*.bash')"
