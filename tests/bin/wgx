#!/usr/bin/env bash
set -euo pipefail
WGX_DIR="${WGX_DIR:-$(cd "$(dirname "$0")/../.." && pwd)}"
export WGX_DIR
export PATH="$WGX_DIR/bin:$WGX_DIR:$PATH"

# Prefer root wgx if it exists, otherwise use PATH
if [[ -x "$WGX_DIR/wgx" ]]; then
  exec "$WGX_DIR/wgx" "$@"
else
  exec wgx "$@"
fi
