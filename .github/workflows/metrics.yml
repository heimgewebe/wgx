name: metrics (contracts-v1)
permissions:
  contents: read
  actions: read
  checks: write
on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"
jobs:
  m:
    uses: heimgewebe/metarepo/.github/workflows/wgx-metrics.yml@contracts-v1
    with:
      post_url: ""
